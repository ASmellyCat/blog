---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostList from '../../components/PostList.astro';

const posts = await getCollection('posts', ({ data }) => data.category === 'footnotes');
---

<BaseLayout title="Footnotes — blog">
  <section class="mb-16">
    <h1 class="text-4xl font-semibold tracking-tight mb-2">Footnotes</h1>
    <p class="text-[var(--color-text-muted)] text-lg">Diary entries.</p>
  </section>
  <PostList posts={posts} />
</BaseLayout>
