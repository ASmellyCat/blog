---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostList from '../../components/PostList.astro';

const posts = await getCollection('posts', ({ data }) => data.category === 'midnight-screenings');
---

<BaseLayout title="Midnight Screenings — blog">
  <section class="mb-16">
    <h1 class="text-4xl font-semibold tracking-tight mb-2">Midnight Screenings</h1>
    <p class="text-[var(--color-text-muted)] text-lg">Dreams.</p>
  </section>
  <PostList posts={posts} />
</BaseLayout>
